/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Agrega aquí los dominios permitidos
    },
  };
  
  export default nextConfig;
  